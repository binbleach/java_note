1、类的访问权限：类如果没有public修饰就是包私有。只有同包下类能访问
2、类中对象和方法的访问权限：
    1、private类内部可访问 < ‘不写默认’同包类可访问 < protected不同包子类可访问 < public任意类可访问
3、修饰符固定搭配，有时候不写不代表默认，编译器会自动加（子类本身是抽象类就可以不重写抽象方法）：
    1、接口中（设计上自上而下）：
        public final static int size = 1080;
        public abstract void take();
        public default void pitu(){}    //java8用于增强接口能力
        private void pitu(){}           //java9用于提取default功能
    2、抽象类（设计上自下而上）（和正常类一样、除了不能实例化）：
        abstract void take();   //可以public、protected、不写（同包类可访问），
    注：为什么抽象类的抽象方法可以用默认/protected修饰，接口的抽象方法只允许public修饰。
        因为protected时，不同包只有子类能访问，违背接口作为公共标准的设计目标。而抽象类的设计目标是提取子类功能，设计自洽。
1、继承（方法重写：访问权限只许放大、不许缩小）：
    1）子类可访问父类 public、protected、包访问权限 的成员；注意：仅是能够访问，并非自身拥有。
    2）子类构造通过super()调用父类构造，super()须放在首行。
        注意：1、无自定义构造，编译器自动生成无参构造。
             2、子类构造默认隐式super()；父类缺少无参构造时，子类需手动调用带参super()。

