一、产生线程安全问题的三个条件
    • 多线程线程并发
    • 有数据共享
    • 有修改数据的操作
    ☣ 解决方法：不让线程并发，同步，排队
    ☠ 局部变量在栈中 ，不共享，不会发送线程安全问题
    ☠ 实例变量在堆中，共享，会发生线程安全问题
    ☠ 静态变量在方法区中，共享，会发生线程安全问题

二、synchronized
    • synchronized与其说是锁与其说是钥匙更好理解，一个对象一个锁，看到synchronized就去锁池里找锁，找到就能进
        运行状态->找锁并放弃抢到的时间片；找到->就绪状态
    • synchronized三种锁法：
        1）代码块上，灵活（对象锁）：synchronized(共享对象){}
        2）实例方法上，共享对象一定是this,不太灵活且可能会扩大范围，不过代码少（对象锁）：synchronized void 实例方法名(){}
        3）静态方法上，表示类锁，保证静态变量的安全，类锁只有一把就算创建了一百个对象也只有一把类锁
    • 避免锁的三种情况：
        1）用局部变量代替成员变量
        2）多创建几个对象避免对象共享。
        3）不行就锁吧，效率低，用户吞吐量低，用户体验差。

三、名词和变量
    • StringBuffer 和 StringBuilder
        ◦ StringBuffer线程安全
        ◦ StringBuilder 线程不安全 局部变量用这个，不用考虑线程安全问题，效率高
    • 守护线程和用户线程
        ◦ 守护线程：就是后台线程执行一些系统级别任务，如垃圾回收、资源管理等。当所有的用户线程结束守护线程才结束，守护线程结束程序停止。
        ◦ 用户线程：就是我们自己创建的普通的程序级别的线程。
        ◦ 查看是否守护线程：Thread.currentThread().isDaemon()
        ◦ 设置守护线程：Thread.currentThread().setDaemon(true)
    • wait 和 sleep
        ◦ sleep() 是 Thread 的静态方法；wait() 方法和notify()方法是Object方法，任何对象都能使用。
        ◦ sleep() 不会释放锁，它也不需要占用锁。wait() 会释放锁，但调用它的前提是当前线程占有锁(即代码要在 synchronized 中)。
        ◦ 它们都可以被 interrupted() 中断。
    • 并行和并发
        ◦ 都是多任务进行，并发需要抢夺cpu资源。
