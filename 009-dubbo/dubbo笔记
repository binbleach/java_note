一、dubbo是阿里开源的一款高性能、轻量级的java rpc框架。
    1、rpc（远程过程调用）：是分布式系统的核心通信技术，让服务间调用如同本地方法般简洁。
        核心：二进制序列化 + 长连接，比HTTP快。
    2、主流rpc通信协议：
       （1）grpc：HTTP/2 + protobuf（跨语言、云原生。强绑定序列）
       （2）dubbo：tcp私有 + hessian2（dubbo2.x主力，序列化还支持Fastjson2、JSON等。）
       （2）triple：HTTP/2 + protobuf（dubbo3.x主力，完全兼容gRPC协议，拥抱云原生。强绑定序列）
    3、grpc框架缺乏微服务周边生态扩展与适配，更适合作为底层的通信协议规范，不适合作为微服务整体解决方案。用的话推荐dubbo框架+gRPC协议 的组合。
    作用：
        1、服务注册发现，支持多种注册中心。
        2、负载均衡、容错机制（超时、重试、熔断限流）。
        3、高效的远程调用，多协议支持（dubbo、http、triple等多种协议）
 二、dubbo提供业务之间的通信协议，zk不提供（参考springcould里的zk部分）。
    dubbo集成zk，消费者不用提供服务名，是直接通过接口定位的。多个提供者可通过分组、版本、服务名等方式区分。
