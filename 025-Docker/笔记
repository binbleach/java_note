一、docker是什么：开源的应用容器引擎。是基于 Linux 内置的 Namespace 和 CGroup 等系统内隔离机制而抽象出来的一种轻虚拟化技术。
                docker早期使用的是LXC(Linux Containers)来实现容器隔离和虚拟化，1.11版本后用了自己写的libcontainer实现
                早期的Docker for Windows也是在windows上创建Linux 虚拟机来使用，windows10才后正式提供Windows容器，底层与linux类似
二、解决了什么痛点：应用运行的环境(切换/配置)问题、应用之间隔离问题
三、docker和传统虚拟机的区别：
    虚拟机也是应用隔离的一种解决方案，相比之下，docker作用与操作系统层虚拟化，更轻量更迅速。传统虚拟机是作用域硬件层的虚拟化，占用大冗余大启动慢。
四、docker三要素
    • 容器：可以把他看做是“应用”和“简易版的linux环境/window环境”（包括root用户权限、进程空间、用户空间和网络空间等）组成的独立程序(用的是宿主机的内核)
    • 镜像：可以看作是创建容器的构造函数，应用不同、应用版本不同、应用配置不同，镜像也就不同
    • 仓库：就是存放镜像的地方，有远程仓库和本地仓库，当我们创建容器时会先去本地仓库寻找镜像，找不到再去远程仓库寻找并下载到本地
五、所谓的build once run anywhere：只是指同一个内核下如 centos、ubunto、debain。而linux容器不能运行windows容器，因为不是一个内核的
六、镜像的底层：
    1、联合文件系统(UnionFS)是镜像的基础，镜像是由一层层的文件系统组成。
    2、镜像的加载原理：
        • 最底层是bootfs这层包含bootloader和kernel，当boot加载完成之后整个内核就都在内存中了，此时内存的使用权已由bootfs转交给内核，此时系统也会卸载bootfs。
        • rootfs：在bootfs层之上，指挂载在容器根目录上的文件系统。包含的就是典型Linux系统中的/dev，/proc，/bin，/etc等标准目录和文件。rootfs 就是各种不同的操作系统发行版，比如Ubuntu, Centos等等。
        • baseimage：基础镜像，如centos、ubunto、debain。提供rootfs服务的镜像（只读的，容器创建后会多分配一层可读层）。
        • scratch：空镜像，除了基于基础镜像创建镜像，也可以基于空镜像创建镜像
        • 在Docker的体系里把这些rootfs叫做容器的镜像。但是，此时的每一层rootfs都是只读read-only的，我们此时还不能对其进行操作。
            当我们创建一个容器，也就是将Docker镜像进行实例化，系统会在一层或是多层read-only的rootfs之上分配一层空的可读可写read-write的rootfs。
            对于相同的基础镜像，在dcoker host里只会存储一份。容器共享共享基础镜像、共享boostfs、kernel。
七、docker命令：
    • 启动docker：systemctl start docker
    • 停止docker：systemctl stop docker
    • 重启docker：systemctl restart docker
    • 查看docker状态：systemctl status docker
    • 查看docker命令帮助文档：docker --help ||或者：docker 具体命令 --help
    • 查看镜像/容器/数据卷所占的空间：docker system df 查看镜像/容器/数据卷所占的空间
八、镜像命令：
    • 查看本地镜像列表：docker images
       展示包含中间层： docker images -a
       只展示镜像id：  docker images -q
    • 查询远程镜像列表：docker search redis
       只展示前n条记录：docker search redis --limit 5
    • 下载最新版镜像(latest)：docker pull redis
       下载指定版本镜像：docker pull redis:6.0.8
    • 删除镜像：docker rmi redis:6.0.8  ||或者：docker rmi 镜像id
       强制删除：docker rmi -f redis:6.0.8   ||或者：dicker rmi -f 镜像id
       删除全部镜像：docker rmi -f $(docker images -qa)
    • 查看镜像详情信息：docker iamge inspect
    • 外网连通的情况下，安装vim：
        ◦ apt-get update    ||更新包管理工具
        ◦ apt-get -y install vim    ||安装vim
    • 生成镜像：docker commit -m="add vim cmd" -a="huangjiabin" 185d905ddf89 myubuntu:3.0.0
        ☣ docker commit -m="提交的描述信息" -a="作者" 容器ID 要创建的目标镜像名:[标签名]
九、容器命令：
    • 启动容器：
       ◦ 无交互模式：docker run redis:6.0.8 （无交互+无法操作）           docker run ubuntu（马上退出）
                                    /bin/bash （马上退出）                        /bin/bash（马上退出）
                                    redis-cli（连接失败+马上退出）
       ◦ 后台运行：docker run -d redis （后台留活）             docker run -d ubuntu （马上退出）
                                   /bin/bash （马上退出）                         /bin/bash （马上退出）
                                   redis-cli（马上退出）
       ◦ 有交互模式：docker run -it redis（其实是redis-server的交互，无法操作）              docker run -it ubuntu （有交互）
                                    /bin/bash （有交互）                         /bin/bash （有交互）
                                    redis-cli（连接失败+有交互）
       ◦ docker启动redis默认启动redis-server，启动ubuntu默认启动bash
       ◦ 容器命名：docker run --name [命名] redis
                         docker run --name=[命名] redis
       ◦ 添加端口映射：docker run -p [主机端口（也就是外部访问的端口）]:[容器端口（是主机寻找应用的端口）] redis
          随机端口映射：docker run -P redis
       ◦ 注：不带版本启动则默认启动最新版，没有镜像则默认到远程仓库下载再启动
            -i:以交互模式运行容器，通常与 -t 同时使用;-t:为容器重新分配一个伪输入终端，通常与 -i 同时使用；
    • 停止容器：
       ◦ 优雅停止：docker stop [容器id/name]
       ◦ 暴力停止：docker kill [容器id/name]
    • 退出容器：
       ◦ 退出并停止：exit
       ◦ 退出不停止：ctrl+p+q
    • 进入容器：
       可交互方式：docker exec -it [容器id/name] redis-cli  ||注：exec会启动新进程、exit不会停止容器。attach效果相反
       不可交互方式：docker attach [容器id/name]     ||他只能进入默认启动
    • 创建容器（run=create+start）：docker creata redis:6.0.8       ||注：其中 -i、-t、-p、-P 选项和 docker run 命令一样
    • 启动容器（run=create+start）：docker start [容器id/name]
       重启容器：docker restart[容器id/name]
    • 删除容器：docker rm [容器id]
       删除所有容器：docker rm -f $(docker ps -a -q) ||或者：docker ps -a -q |xargs docker rm
       删除最新2个容器：docker rm -f $(docker ps -n 2)
    • 查看运行中容器列表：docker ps
       查看所有容器列表(包含非运行)：docker ps -a
       查看2个容器列表(包含非运行，从最新到最后创建获取)：docker ps -n 2
       查看上一个创建容器：docker ps -l
       查看容器id列表(不包含非运行)：docker ps -q
    • 查看容器详细信息：docker inspect [容器id/name]
    • 查看容器linux版本信息：在容器/bin/bash中输入命令 cat /etc/issue
    • 拷贝容器上的文件到主机：docker cp dc8679bbc0c5:/data/test.txt /opt/test.txt   |dc8679bbc0c5是容器id,/data/test.txt是文件容器内路径，/opt/test.txt是主机路径
    //用以下两个导出导入redis正常，镜像运行却异常，可以运行/bin/bash但是文件丢失。ubuntu镜像运行时也异常，但是运行/bin/bash成功且文件不丢失
    • 导出整个容器出docker到主机：docker export dc8679bbc0c5 > /opt/abcd.tar.gz    |dc8679bbc0c5是容器id，/opt/abcd.tar是导出后文件路径
    • 将导出到主机的容器导入进docker成镜像：cat /opt/abcd.tar.gz | docker import - myredis:6.0.8    | /opt/abcd.tar是主机下容器路径，myredis:6.0.8为镜像名称和版本号
十、玩转阿里云
    • 创建阿里云仓库：先创建命名空间再创建镜像仓库
    • 登录拉取镜像推送镜像：在仓库管理里复制就好了
    • 阿里云库拉取下的镜像名称：registry.cn-hangzhou.aliyuncs.com/huangjiabin_docker_learning/myubuntu1
十一、私有库搭建
    • 下载私有库镜像：docker pull registry
    • 运行私有库，并设置私有库镜像存放到docker数据卷：docker run -d -p 5000:5000  -v /huse/docker/myregistry/:/tmp/registry --privileged=true registry
    • 设置开放5000端口："insecure-registries": ["192.168.8.128:5000"]
    • 验证私有库镜像（此时不存在镜像）：curl -XGET http://192.168.8.128:5000/v2/_catalog
    • 给镜像打标签：docker tag  myubuntu:2.0.0  192.168.8.128:5000/myubuntu:2.0.0
    • 推送镜像到私有库：docker push 192.168.8.128:5000/myubuntu:2.0.0
    • 再次验证私有库镜像（此时会存在镜像）：curl -XGET http://192.168.8.128:5000/v2/_catalog
    • 拉取私有库镜像：docker pull 192.168.8.128:5000/myubuntu:2.0.0
    • 私有库拉取的镜像名称：192.168.8.128:5000/myubuntu
十二、数据卷（持久化容器数据）
    • 挂载数据卷：docker run -it --name u1 --privileged=true -v/tmp/myHostData:/tmp/myDockerData:rw ubuntu /bin/bash
        ◦ --privileged=true 是给 容器内的root拥有真正的root权限
        ◦ /tmp/myHostData 是主机目录（没有会自动创建）
        ◦ /tmp/myDockerData 是容器目录（没有会自动创建）
        ◦ :rw 表示容器可读可写（默认的可忽略）
           :ro 表示容器只能读，不可以对宿主机进行写操作
        ◦ 当我们在启动的容器中 的/tmp/myDockerData目录下创建一个文件（touch test.txt），他将会同步到宿主机的 /tmp/myHostData目录中去
    • 查看容器挂载情况：命令 docker inspect [容器id/name] 中 Mounts属性
    • 继承数据卷：docker run -it --name u2 --privileged=true --volumes-from u1 ubuntu
十三、安装软件
    • 安装tomcat：docker run -d -p 8080:8080 --name tc tomcat
        ◦ 新版本还不能直接访问8080，因为webapps文件里是空的。需要：
            1）docker exec -it tc bash
            2）rm -r webapps
            3）mv webapps.dist webapps
        ◦ 旧版本安装：docker run -d -p 8080:8080 --name mytomcat8 billygoo/tomcat8-jdk8
              ||或者： docker run -d -p 8080:8080 --name tc tomcat:8.0.53
    • 安装mysql：docker run -p 3306:3306 -e MYSQL_ROOT_PASSWORD=root -d --name ms mysql:5.7
        ◦ 操作mysql：docker exec -it ms bash ——> msyql -uroot -proot
        ◦ docker中mysql乱码隐患：容器上mysql执行 SHOW VARIABLES LIKE 'character%'; 可以看到字符集全是 latin1
    • 安装mysql实战版本：docker run -d -p 3306:3306 --privileged=true
                        -v /huse/docker/mysql/log:/var/log/mysql
                        -v /huse/docker/mysql/data:/var/lib/mysql
                        -v /huse/docker/mysql/conf:/etc/mysql/conf.d
                        -e MYSQL_ROOT_PASSWORD=root  --name ms mysql:5.7
        ◦解决乱码隐患：配置 /huse/docker/mysql/conf/my.cnf 文件，内容：
            [client]
            default_character_set=utf8
            [mysqld]
            collation_server = utf8_general_ci
            character_set_server = utf8
            ★需要重启容器才生效：docker restart ms
        ◦ 我们挂载了数据卷，即使删掉容器数据也是可恢复的
    • 安装redis：docker run  -p 6379:6379 --name re --privileged=true
                -v /opt/docker/dataVolumes/redis/redis.conf:/etc/redis/redis.conf
                -v /opt/docker/dataVolumes/redis/data:/data
                -d redis:6.0.8 redis-server /etc/redis/redis.conf --appendonly yes
        ◦ --appendonly yes 是开启数据持久化

十四、高级搭建
    一）mysql高级安装：docker run -p 3307:3306 --name mysql-master
                    -v /opt/docker/dataVolumes/advanced/mysql/mysql-master/log:/var/log/mysql
                    -v /opt/docker/dataVolumes/advanced/mysql/mysql-master/data:/var/lib/mysql
                    -v /opt/docker/dataVolumes/advanced/mysql/mysql-master/conf:/etc/mysql/conf.d
                    -e MYSQL_ROOT_PASSWORD=root -d mysql:5.7
        1、 在对应的 /opt/docker/dataVolumes/advanced/mysql/mysql-master/conf 目录下新增 my.cnf 配置文件，内容：
            [mysqld]
            ## 设置server_id，同一局域网中需要唯一
            server_id=101
            ## 指定不需要同步的数据库名称
            binlog-ignore-db=mysql
            ## 开启二进制日志功能
            log-bin=mall-mysql-bin
            ## 设置二进制日志使用内存大小（事务）
            binlog_cache_size=1M
            ## 设置使用的二进制日志格式（mixed,statement,row）
            binlog_format=mixed
            ## 二进制日志过期清理时间。默认值为0，表示不自动清理。
            expire_logs_days=7
            ## 跳过主从复制中遇到的所有错误或指定类型的错误，避免slave端复制中断。
            ## 如：1062错误是指一些主键重复，1032错误是因为主从数据库数据不一致
            slave_skip_errors=1062
            ☣ 需要重启生效：docker restart mysql-master
        2、创建数据同步用户：
            1）CREATE USER 'slave'@'%' IDENTIFIED BY '123456';
            2）GRANT REPLICATION SLAVE, REPLICATION CLIENT ON *.* TO 'slave'@'%';
        3、启动从机mysql：docker run -p 3308:3306 --name mysql-slave
                    -v /opt/docker/dataVolumes/advanced/mysql/mysql-slave/log:/var/log/mysql
                    -v /opt/docker/dataVolumes/advanced/mysql/mysql-slave/data:/var/lib/mysql
                    -v /opt/docker/dataVolumes/advanced/mysql/mysql-slave/conf:/etc/mysql/conf.d
                    -e MYSQL_ROOT_PASSWORD=root
                    -d mysql:5.7
        4、从机配置：/opt/docker/dataVolumes/advanced/mysql/mysql-slave/conf 下 my.cnf 文件，内容：
            [mysqld]
            ## 设置server_id，同一局域网中需要唯一
            server_id=102
            ## 指定不需要同步的数据库名称
            binlog-ignore-db=mysql
            ## 开启二进制日志功能，以备Slave作为其它数据库实例的Master时使用
            log-bin=mall-mysql-slave1-bin
            ## 设置二进制日志使用内存大小（事务）
            binlog_cache_size=1M
            ## 设置使用的二进制日志格式（mixed,statement,row）
            binlog_format=mixed
            ## 二进制日志过期清理时间。默认值为0，表示不自动清理。
            expire_logs_days=7
            ## 跳过主从复制中遇到的所有错误或指定类型的错误，避免slave端复制中断。
            ## 如：1062错误是指一些主键重复，1032错误是因为主从数据库数据不一致
            slave_skip_errors=1062
            ## relay_log配置中继日志
            relay_log=mall-mysql-relay-bin
            ## log_slave_updates表示slave将复制事件写进自己的二进制日志
            log_slave_updates=1
            ## slave设置为只读（具有super权限的用户除外）
            read_only=1
        5、主数据库查看同步状态：show master status;
        6、从数据库配置主从复制：change master to master_host='192.168.8.128', master_user='slave', master_password='123456',
            master_port=3307, master_log_file='mall-mysql-bin.000005', master_log_pos=617, master_connect_retry=30;
        7、从数据库查看同步状态：show slave status \G;
            ◦ \G ： 展示状态为键值对（非表格）
            ◦ Slave_SQL_Running 和 Slave_IO_Running ：是否开始主从复制
        8、从数据库开启主从复制：start slave;
    二）redis高级搭建：
        1、redis集群搭建方案：
            1） 哈希取余分区：hash(key)%节点数。
                优点：简单有效，只需要预估好数据规划好节点，就能保证一段时间的数据支撑。 缺点：不过不支持节点变动
            2） 一致性哈希算法分区：
                步骤：① 性算法构建一致性hash环（%2^32） ② 服务器IP节点映射，hash(ip) ③key落到服务器的落键规则，hash(key)
                优点：容错性，扩容性。 缺点：节点太少，数据倾斜，请求分配不均
            3） 哈希槽分区：
                特征：一个集群只能有16384(2^14)个哈希槽，区间0-16383，每个key通过CRC16校验后对2^14取模来决定存放在哪个槽。
                    集群每个节点负责一部分hash槽。
                    ☣ 为什么是16384个，因为集群节点不能超过1000造成网络拥堵，1000个节点16384个槽位够用了。
                        如果槽位是65536(2^16)个，发送心跳包过于庞大
                优点：解决均匀分配问题
        2、启动6个redis：
            ◦ docker run -d --name redis-node-1 --net host --privileged=true
                -v /opt/docker/dataVolumes/advanced/redis/data/share/redis-node-1:/data
                redis:6.0.8 --cluster-enabled yes --appendonly yes --port 6381
            ◦ docker run -d --name redis-node-2 --net host --privileged=true
                -v /opt/docker/dataVolumes/advanced/redis/data/share/redis-node-2:/data
                redis:6.0.8 --cluster-enabled yes --appendonly yes --port 6382
            ◦ docker run -d --name redis-node-3 --net host --privileged=true
                -v /opt/docker/dataVolumes/advanced/redis/data/share/redis-node-3:/data
                redis:6.0.8 --cluster-enabled yes --appendonly yes --port 6383
            ◦ docker run -d --name redis-node-4 --net host --privileged=true
                -v /opt/docker/dataVolumes/advanced/redis/data/share/redis-node-4:/data
                redis:6.0.8 --cluster-enabled yes --appendonly yes --port 6384
            ◦ docker run -d --name redis-node-5 --net host --privileged=true
                -v /opt/docker/dataVolumes/advanced/redis/data/share/redis-node-5:/data
                redis:6.0.8 --cluster-enabled yes --appendonly yes --port 6385
            ◦ docker run -d --name redis-node-6 --net host --privileged=true
                -v /opt/docker/dataVolumes/advanced/redis/data/share/redis-node-6:/data
                redis:6.0.8 --cluster-enabled yes --appendonly yes --port 6386
        3、构建主从关系，进入 redis-node-1 容器执行：
            redis-cli --cluster create
            192.168.8.128:6381
            192.168.8.128:6382
            192.168.8.128:6383
            192.168.8.128:6384
            192.168.8.128:6385
            192.168.8.128:6386 --cluster-replicas 1
            ☣ Can I set the above configuration? (type 'yes' to accept): yes
            ☠ 3主3从搞定
        4、查看集群状态：
            1）在 redis-node-1的bash内执行：redis-cli -p 6381 -c
                ☠ 必须要加端口，否则进不去
                ☠ 加 -c ，当我们读写数据时，key值匹配的插槽非当前节点，可以路由到对应节点。
            2）查看集群信息：cluster info
            3）查看集群节点：cluster nodes
        ◦ 查看集群信息：在 redis-node-1 的 bash 内执行：redis-cli --cluster check 192.168.8.128:6381
    三）redis的扩容（在redis高级搭建基础上）：
        1、启动两个redis：
            ◦ docker run -d --name redis-node-7 --net host --privileged=true
                -v /opt/docker/dataVolumes/advanced/redis/data/share/redis-node-7:/data
                redis:6.0.8 --cluster-enabled yes --appendonly yes --port 6387
            ◦ docker run -d --name redis-node-8 --net host --privileged=true
                -v /opt/docker/dataVolumes/advanced/redis/data/share/redis-node-8:/data
                redis:6.0.8 --cluster-enabled yes --appendonly yes --port 6388
        2、将新增的6387节点(空槽号)作为master节点加入原集群：
            在redis-node-7的bash内执行：redis-cli --cluster add-node 192.168.8.128:6387 192.168.8.128:6381
            ☠ 此时 redis-cli --cluster check 192.168.8.128:6381 可查看 6387 是没有槽号的 ：
                192.168.8.128:6387 (c6942044...) -> 0 keys | 0 slots | 0 slaves.
        3、给集群从新分派槽号：redis-cli --cluster reshard 192.168.8.128:6381
            ☠ 1 to 16384 -> How many slots do you want to move (from 1 to 16384)? 4096
                ☣ 16384 ÷ 4= 4096
            ☣ c6942044df076e7eb94fc3e9234adb0eb286b649 192.168.8.128:6387 ->
                What is the receiving node ID? c6942044df076e7eb94fc3e9234adb0eb286b649
            ☣ Source node #1: all
            ☠ 此时 redis-cli --cluster check 192.168.8.128:6381 可查看 6387 是有槽号的 ：
                192.168.8.128:6387 (c6942044...) -> 0 keys | 4096 slots | 0 slaves.
            ☠ 为什么6387是3个新的区间，以前的还是连续？
              重新分配成本太高，所以前3家各自匀出来一部分，从6381/6382/6383三个旧节点分别匀出1364个坑位给新节点6387
        4、给新扩容主机6387分配从机6388：
            redis-cli --cluster add-node 192.168.8.128:6388 192.168.8.128:6387 --cluster-slave
             --cluster-master-id c6942044df076e7eb94fc3e9234adb0eb286b649
    四）redis的缩容（在扩容的基础上）
        1、删除 6388的节点：redis-cli --cluster del-node 192.168.8.128:6388 9443330818883f4d5aa35e37388b65595996e7db
        2、将 6387 的槽号清空给6381：redis-cli --cluster reshard 192.168.8.128:6381
            ☣ How many slots do you want to move (from 1 to 16384)? 4096
            ☣ What is the receiving node ID? 9b50684781d67f4a3f50b4825fd5b88bcb19f1b9
            ☣ Source node #1: c6942044df076e7eb94fc3e9234adb0eb286b649
            ☣ Source node #2: done
            ☣ Do you want to proceed with the proposed reshard plan (yes/no)? yes
        3、将 6387 节点删除：redis-cli --cluster del-node 192.168.8.128:6387 c6942044df076e7eb94fc3e9234adb0eb286b649

十五、Dockerfile
    一）构建3步骤：
        1、编写 Dockerfile文件
        2、docker build命令构建镜像
        3、docker run依镜像运行容器实例
    二）Dockerfile内容基础知识：
        1、每条保留字指令都必须为大写字母 且 后面要跟随至少一个参数
        2、指令按照从上到下，顺序执行
        3、#表示注释
        4、每条指令都会创建一个新的镜像层并对镜像进行提交
    三）Dockerfile保留字：
        ◦ FROM ：基础镜像，当前新镜像是基于哪个镜像的，指定一个已经存在的镜像作为模板，第一条必须是from
        ◦ MAINTAINER ：镜像维护者的姓名和邮箱地址
        ◦ RUN ：容器构建时需要运行的命令，在 docker build时运行。
            ◦ RUN格式：1）shell ：RUN <命令行命令> ，如：RUN yum -y install vim
                        ☣ <命令行命令>等同于在终端操作的命令
                      2）exec ：RUN ["可执行文件","参数1","参数2"] ：RUN["./test.php","dev","offline"]
                        ☣ 等价于 RUN ./test.php dev offline
        ◦ EXPOSE ：当前容器对外暴露出的端口
        ◦ WORKDIR ：指定在创建容器后，终端默认登陆的进来工作目录，一个落脚点（bash进入的目录）
        ◦ USER ：指定该镜像以什么样的用户去执行，如果都不指定，默认是root
        ◦ ENV ：用来在构建镜像过程中设置环境变量
                ☣ 这个环境变量可以在后续的任何RUN指令中使用，这就如同在命令前面指定了环境变量前缀一样：ENV MY_PATH /usr/mytest
                  也可以在其它指令中直接使用这些环境变量，比如：WORKDIR $MY_PATH
        ◦ ADD ：将宿主机目录下的文件拷贝进镜像且会自动处理URL和解压tar压缩包
        ◦ COPY ：类似ADD，拷贝文件和目录到镜像中。将从构建上下文目录中 <源路径> 的文件/目录复制到新的一层的镜像内的 <目标路径> 位置
                格式：
                    1）Shell ：COPY src dest
                    2）exec ： COPY ["src", "dest"]
                注意：
                    1）<src源路径>：源文件或者源目录
                    2）<dest目标路径>：容器内的指定路径，该路径不用事先建好，路径不存在的话，会自动创建。
        ◦ VOLUME ：容器数据卷，用于数据保存和持久化工作
        ◦ CMD ：指定容器启动后的要干的事情
                注意：Dockerfile 中可以有多个 CMD 指令，但只有最后一个生效，CMD 会被 docker run 之后的参数替换
                格式 ：
                    1）Shell ：CMD <命令>
                    2）exec  ：CMD ["可执行文件","参数1","参数2"]
                    3）当配合 ENTRYPOINT 命令使用时，只能为其传参 ：CMD ["参数1","参数2"]
                实例：
                    原本的tomcat镜像：CMD ["catalina.sh","run"] 当启动时 docker run -it -p 8080:8080 tomcat /bin/bash
                    那我们加的 /bin/bash就会把 CMD覆盖成 ：CMD ["/bin/bash","run"] 导致我们启动容器失败
        ◦ ENTRYPOINT：也是用来指定一个容器启动时要运行的命令
                注意：
                    1）类似于 CMD 指令，但是ENTRYPOINT不会被docker run后面的命令覆盖，而且这些命令行参数会被当作参数
                        送给 ENTRYPOINT 指令指定的程序
                    2）如果 Dockerfile 中如果存在多个 ENTRYPOINT 指令，仅最后一个生效。
                实例：
                    ENTRYPOINT ["nginx","-c"] #定参
                    CMD ["/etc/nginx/nginx.conf"] #变参
                    启动容器时：docker run nginx                          |实际：nginx -c /etc/nginx/nginx.conf
                            ：docker run nginx -c /etc/nginx/new.conf   |实际：nginx -c /etc/nginx/new.conf
    四）使用Dockerfile创建镜像
        1、在 /opt/docker/myDockerfile 目录下放下jdk
        2、在 /opt/docker/myDockerfile 目录下创建文件 Dockerfile，内容：
            FROM centos:7
            MAINTAINER hjbin<2901038144@qq.com>

            ENV MYPATH /usr/local
            WORKDIR $MYPATH

            #安装vim编辑器
            RUN yum -y install vim
            #安装ifconfig命令查看网络IP
            RUN yum -y install net-tools
            #安装java8及lib库
            RUN yum -y install glibc.i686
            RUN mkdir /usr/local/java
            #ADD 是相对路径jar,把jdk-8u171-linux-x64.tar.gz添加到容器中,安装包必须要和Dockerfile文件在同一位置
            ADD jdk-8u381-linux-x64.tar.gz /usr/local/java/
            #配置java环境变量
            ENV JAVA_HOME /usr/local/java/jdk1.8.0_381
            ENV JRE_HOME $JAVA_HOME/jre
            ENV CLASSPATH $JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar:$JRE_HOME/lib:$CLASSPATH
            ENV PATH $JAVA_HOME/bin:$PATH

            EXPOSE 80

            CMD echo $MYPATH
            CMD echo "success--------------ok"
            CMD /bin/bash
        3、执行命令（注意结尾有个点）：docker build -t centosjava8:1.0.0 .
    五）虚悬镜像：仓库和标签都是none的镜像，有时候我们Dockerfile发生错误时会创建，有时候没对基础镜像做增强时会创建
        1、查看虚悬镜像：docker image ls -f dangling=true
        2、删除虚悬镜像：docker image prune
    六）将springboot的jar打成镜像
        1、在 /opt/docker/dockerAndJar 目录下放下springboot项目打的jar包
            注意jar包的pom.xml文件需加上：
            <build>
                    <plugins>
                        <plugin>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-maven-plugin</artifactId>
                        </plugin>
                    </plugins>
                </build>
            否则无法找到main无法启动
        2、在 /opt/docker/dockerAndJar 目录下创建文件 Dockerfile，内容：
            # 基础镜像使用java
            FROM openjdk:8
            # 作者
            MAINTAINER hjbin<2901038144@qq.com>
            # VOLUME 指定临时文件目录为/tmp，在主机/var/lib/docker目录下创建了一个临时文件并链接到容器的/tmp
            VOLUME /tmp
            # 将jar包添加到容器中并更名为hjbin_docker.jar
            ADD docker-1.0.0.jar hjbin_docker.jar
            # 运行jar包
            RUN bash -c 'touch /hjbin_docker.jar'
            ENTRYPOINT ["java","-jar","/hjbin_docker.jar"]
            #暴露6001端口作为微服务
            EXPOSE 6001
        3、执行命令（注意结尾有个点）：docker build -t docker_jar:1.0.0 .
        4、启动容器：docker run -d -p 6001:6001 docker_jar:1.0.0
        4、测试：curl 127.0.0.1:6001/order/docker

十六、docker网络
    一）：
        1、linxu的默认网络接口：ens33、lo、virbr0
        2、docker启动后会新增一个网络接口：docker0
        4、docker默认创建的网络：host、none、bridge
        3、查看网络：docker network ls
        5、查看网络详情：docker network inspect  XXX网络名字
        6、创建网络：docker network create XXX网络名字
        7、删除网络：docker network rm XXX网络名字
    二）：docker网络模式介绍：
        1、bridge：为每一个容器分配、设置IP 等，并将容器连接到一个 docker0虚拟网桥，默认为该模式（我们创建网络默认用的这个模式）。
            Docker 服务默认会创建一个 docker0 网桥（其上有一个 docker0 内部接口），该桥接网络的名称为docker0，
            它在内核层连通了其他的物理或虚拟网卡，这就将所有容器和本地主机都放到同一个物理网络。
            Docker 默认指定了 docker0 接口 的 IP 地址和子网掩码，让主机和容器之间可以通过网桥相互通信。
        2、host：容器和宿主机共享Network namespace。容器将不会虚拟出自己的网卡，配置自己的IP等，而是使用宿主机的IP 和端口。
        3、容器有独立的 Network namespace，但并没有对其进行任何网络设置，如分配 veth pair 和网桥连接，IP 等。
        4、新创建的容器不会创建自己的网卡和配置自己的IP，而是和一个指定的容器共享IP、端口范围等。
    三）：容器间网络通信：
        1、我们在redis高级时设置的 --net host 表示用的host网络，复用的都是主机的属于同一个网段，相互能通信，
            而默认的bridge却不行
        2、解决bridge不行：
            1）创建自定义网络，模式还是bridge：docker network create hjbin_network
            2）启动容器时配置成自定义网络：
                docker run -d -p 8081:8080 --network hjbin_network --name tomcat81 billygoo/tomcat8-jdk8
                docker run -d -p 8082:8080 --network hjbin_network --name tomcat82 billygoo/tomcat8-jdk8
            3）通过容器名访问：
                ① docker exec -it tomcate81 bash
                ② ping tomcat82 #之前用的不通过，现在通过了

