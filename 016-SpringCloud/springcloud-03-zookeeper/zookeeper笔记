一、zookeeper：是一个分布式协调服务
    1、服务注册与发现：提供者注册、消费者拉取；服务名替代IP，地址变更无感知、不影响调用
    2、配置中心：统一管控配置，一处修改，全局服务实时生效。
    3、分布式锁：多服务抢资源时互斥，保证同一时间只有一个服务执行。
    4、集群管理：集群选主、节点状态监控、感知。
二、zookeeper服务注册发现流程：
    1、提供者启动连接zk，创建临时节点（断开自动删除），节点内写入自身地址、元数据（版本、权重、协议dubbo/grpc/http、环境）。
    2、消费者启动连接zk，拉取服务列表，将服务名和ip+端口映射到本地内存；注册watcher监听（监听节点状态）。
    3、通过服务名调用：调用时消费者通过负载均衡组件（如Ribbon/dubbo自带/LoadBalanced）从本地内存的服务地址列表中选一个，
        直接通过对应协议发起网络请求调用提供者（zk不参与服务调用和通信）
三、zookeeper的数据模型：类似一个树形文件系统
    ZooKeeper 集群
    ├─ /services        【服务注册区：临时节点】
    │  └─ user-service
    │     ├─ 192.168.1.10:8080 → {IP,端口,版本,权重,协议} 【元数据】
    │     └─ 192.168.1.11:8080 → {IP,端口,版本,权重,协议}
    └─ /config          【配置中心区：永久节点】
       ├─ common        全局配置
       └─ user-service  服务私有配置
四、zookeeper协议：
    - 客户端与ZK服务端通信：ZK私有协议，非ZAB协议
    - ZAB协议：仅ZK集群内部使用，负责数据同步、Leader选举，不对外服务
三、zookeeper锁：
