
server:
  port: 8001

spring:
  application:
    name: bbb
  cloud:
    consul:
      host: localhost #consul注册中心主机名称
      port: 8500    #consul注册中心端口
      discovery:
        service-name: aaa  #微服务名称
        #hostname: 127.0.0.1
