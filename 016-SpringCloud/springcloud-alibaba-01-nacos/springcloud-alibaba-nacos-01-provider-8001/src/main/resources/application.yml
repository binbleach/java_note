server:
  port: 8001

spring:
  application:
    name: springcloud-alibaba-nacos-provider
  profiles:
    active: test
#      active: dev #表示开发环境
  cloud:
    nacos:
      discovery:
#        server-addr: localhost:8848 #配置Nacos地址，默认
        server-addr: 192.168.8.128:1111 #配置nginx，由nginx负载到nacos集群
management:
  endpoints:
    web:
      exposure:
        include: '*'
