# nacos配置
#server:
#  port: 8001

spring:
#  application:
#    name: springcloud-alibaba-nacos-provider
#  profiles:
#    active: dev #表示开发环境
  cloud:
    nacos:
#      discovery:
#        server-addr: localhost:8848 #Nacos服务注册中心地址
      config:
        server-addr: localhost:8848 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
#        group: DEFAULT_GROUP  #默认，区分大小写
        group: TEST_GROUP
#        namespace: public    #默认
        namespace: test


#management:
#  endpoints:
#    web:
#      exposure:
#        include: '*'

#${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}
#springcloud-alibaba-nacos-provider-dev.yaml
